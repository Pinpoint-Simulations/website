function TosParagraph({title, description}){
    return (
        <div className="tosparagraph">
            <h3 className="tosname">{title}</h3>
            <p className="tosdesc">{description}</p>
        </div>
    )
}
export default TosParagraph;